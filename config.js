/* Supabase connection.
   Both values are safe to commit — the publishable key is a public client key,
   and every table is guarded by row-level security (see supabase/schema.sql).
   Leave them empty and the site runs in local mode off collection.json. */
window.SUPABASE_URL = 'https://gaetgjyllndwssngaqih.supabase.co';
window.SUPABASE_ANON_KEY = 'sb_publishable_vyY87IQ7Vw4h6UthfMiuyw_rc4Qghr2';
